# Calibration report archive

Files in this directory are immutable outputs from `rank-rent calibrate run`. Commit reviewed
baseline and pre-release reports so scoring changes can be compared to the evidence and
configuration that were active at the time. The CLI refuses to overwrite an existing report.
